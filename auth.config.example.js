/**
 * Firebase web app config (client-side). Copy this file to auth.config.js
 * in the site root and fill in values from Firebase Console.
 * auth.config.js is gitignored — do not commit real keys.
 * After copying, add `<script src="./auth.config.js"></script>` immediately
 * before `auth.min.js` on pages where you want sign-in (index omits it by default).
 */
window.__NV_FIREBASE_CONFIG__ = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: ''
};
